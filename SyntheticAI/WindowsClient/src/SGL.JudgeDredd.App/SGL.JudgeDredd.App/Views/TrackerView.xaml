<UserControl x:Class="SGL.JudgeDredd.App.Views.TrackerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900"
             Background="{StaticResource BackgroundBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Tracker Monitor" Style="{StaticResource HeadingTextStyle}"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Start Monitoring" Style="{StaticResource PrimaryButtonStyle}"
                        Command="{Binding StartMonitoringCommand}" Padding="16,8" Margin="0,0,8,0"/>
                <Button Content="Stop" Style="{StaticResource DangerButtonStyle}"
                        Command="{Binding StopMonitoringCommand}" Padding="16,8"/>
            </StackPanel>
        </Grid>

        <!-- Status bar -->
        <Border Grid.Row="1" Style="{StaticResource SurfaceBorderStyle}" Margin="0,0,0,16">
            <Grid>
                <TextBlock Text="{Binding MonitorStatus}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
                <TextBlock HorizontalAlignment="Right" FontSize="12" FontWeight="Bold">
                    <Run Text="Trackers: " Foreground="{StaticResource TextSecondaryBrush}"/>
                    <Run Text="{Binding TrackersDetected}" Foreground="{StaticResource DangerBrush}"/>
                </TextBlock>
            </Grid>
        </Border>

        <!-- Info card -->
        <Border Grid.Row="2" Padding="12,8" Background="{StaticResource SurfaceBrush}"
                CornerRadius="{StaticResource DefaultCornerRadius}" Margin="0,0,0,16"
                BorderBrush="{StaticResource WarningBrush}" BorderThickness="1">
            <TextBlock TextWrapping="Wrap" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}">
                This monitor detects telemetry services, tracking processes, spyware, and screen monitoring
                from Microsoft, Google, Adobe, and third-party sources. Click Start to scan running processes
                for known trackers. Use Kill to terminate detected trackers.
            </TextBlock>
        </Border>

        <!-- Tracker list -->
        <DataGrid Grid.Row="3" ItemsSource="{Binding Trackers}"
                  IsReadOnly="True" CanUserSortColumns="True"
                  VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Auto"
                  AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Process" Binding="{Binding ProcessName}" Width="140"/>
                <DataGridTextColumn Header="PID" Binding="{Binding Pid}" Width="60"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="150"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                <DataGridTextColumn Header="Memory" Binding="{Binding MemoryUsage}" Width="80"/>
                <DataGridTextColumn Header="Detected" Binding="{Binding DetectedAt, StringFormat='{}{0:HH:mm:ss}'}" Width="80"/>
                <DataGridTemplateColumn Header="Actions" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Kill" Style="{StaticResource DangerButtonStyle}"
                                    Padding="8,2" FontSize="11"
                                    Command="{Binding DataContext.KillTrackerCommand,
                                        RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
