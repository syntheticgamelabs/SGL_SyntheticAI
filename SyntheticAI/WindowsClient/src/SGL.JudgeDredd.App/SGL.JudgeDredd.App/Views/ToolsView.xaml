<UserControl x:Class="SGL.JudgeDredd.App.Views.ToolsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:SGL.JudgeDredd.App.Views"
             xmlns:vm="clr-namespace:SGL.JudgeDredd.App.ViewModels">
    <Grid>
        <TabControl SelectedIndex="{Binding SelectedTabIndex}"
                    Background="Transparent" BorderThickness="0">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Foreground" Value="#F0E6D0"/>
                    <Setter Property="Background" Value="#3A3020"/>
                    <Setter Property="Padding" Value="16,8"/>
                    <Setter Property="FontSize" Value="13"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border x:Name="Border" Background="{TemplateBinding Background}"
                                        CornerRadius="6,6,0,0" Padding="{TemplateBinding Padding}" Margin="2,0">
                                    <ContentPresenter ContentSource="Header"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="#4A3D2A"/>
                                        <Setter Property="Foreground" Value="#FFD700"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="#3D3020"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>

            <TabItem Header="Git">
                <local:GitView DataContext="{Binding GitVm}"/>
            </TabItem>
            <TabItem Header="VPN">
                <local:VpnView DataContext="{Binding VpnVm}"/>
            </TabItem>
            <TabItem Header="Speed Test">
                <local:TestSpeedView DataContext="{Binding TestSpeedVm}"/>
            </TabItem>
            <TabItem Header="System Cleaner">
                <local:SystemCleanerView DataContext="{Binding CleanerVm}"/>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
