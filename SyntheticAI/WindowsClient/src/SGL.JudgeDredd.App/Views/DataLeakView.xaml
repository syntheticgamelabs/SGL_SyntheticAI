<UserControl x:Class="SGL.JudgeDredd.App.Views.DataLeakView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900"
             Background="{StaticResource BackgroundBrush}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="200"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Data Leak Monitor" Style="{StaticResource HeadingTextStyle}"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Start Monitoring" Style="{StaticResource PrimaryButtonStyle}"
                        Command="{Binding StartMonitoringCommand}" Padding="16,8" Margin="0,0,8,0"/>
                <Button Content="Stop" Style="{StaticResource DangerButtonStyle}"
                        Command="{Binding StopMonitoringCommand}" Padding="16,8"/>
            </StackPanel>
        </Grid>

        <!-- Status -->
        <Border Grid.Row="1" Style="{StaticResource SurfaceBorderStyle}" Margin="0,0,0,12">
            <TextBlock Text="{Binding MonitorStatus}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
        </Border>

        <!-- Network rates -->
        <Border Grid.Row="2" Style="{StaticResource SurfaceBorderStyle}" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                    <TextBlock Text="ACTIVE TRANSFERS" FontSize="10" Foreground="{StaticResource TextSecondaryBrush}"
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding ActiveTransfers}" FontSize="24" FontWeight="Bold"
                               Foreground="{StaticResource PrimaryBrush}" HorizontalAlignment="Center"/>
                </StackPanel>
                <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                    <TextBlock Text="TOTAL UPLOADED" FontSize="10" Foreground="{StaticResource TextSecondaryBrush}"
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding TotalUploadRate}" FontSize="18" FontWeight="Bold"
                               Foreground="{StaticResource WarningBrush}" HorizontalAlignment="Center"/>
                </StackPanel>
                <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                    <TextBlock Text="TOTAL DOWNLOADED" FontSize="10" Foreground="{StaticResource TextSecondaryBrush}"
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding TotalDownloadRate}" FontSize="18" FontWeight="Bold"
                               Foreground="{StaticResource SuccessBrush}" HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Active transfers label -->
        <TextBlock Grid.Row="3" Text="Active Data Transfers" Style="{StaticResource SubheadingTextStyle}" Margin="0,0,0,8"/>

        <!-- Active transfers grid -->
        <DataGrid Grid.Row="4" ItemsSource="{Binding ActiveTransfers_}"
                  IsReadOnly="True" CanUserSortColumns="True"
                  VerticalScrollBarVisibility="Auto"
                  AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Process" Binding="{Binding ProcessName}" Width="120"/>
                <DataGridTextColumn Header="PID" Binding="{Binding Pid}" Width="60"/>
                <DataGridTextColumn Header="Direction" Binding="{Binding Direction}" Width="100"/>
                <DataGridTextColumn Header="Source" Binding="{Binding SourcePath}" Width="*"/>
                <DataGridTextColumn Header="Destination" Binding="{Binding DestinationInfo}" Width="140"/>
                <DataGridTextColumn Header="Memory" Binding="{Binding MemoryUsage}" Width="80"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="70"/>
                <DataGridTemplateColumn Header="Action" Width="70">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Kill" Padding="8,2" FontSize="11"
                                    Style="{StaticResource DangerButtonStyle}"
                                    Command="{Binding DataContext.KillProcessCommand,
                                        RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Time" Binding="{Binding DetectedAt, StringFormat='{}{0:HH:mm:ss}'}" Width="70"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- History label -->
        <Grid Grid.Row="5" Margin="0,12,0,8">
            <TextBlock Text="Transfer History" Style="{StaticResource SubheadingTextStyle}"/>
            <Button Content="Clear History" Style="{StaticResource SecondaryButtonStyle}"
                    Command="{Binding ClearHistoryCommand}" HorizontalAlignment="Right"
                    Padding="12,4" FontSize="11"/>
        </Grid>

        <!-- History list -->
        <DataGrid Grid.Row="6" ItemsSource="{Binding TransferHistory}"
                  IsReadOnly="True" CanUserSortColumns="True"
                  VerticalScrollBarVisibility="Auto"
                  AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Time" Binding="{Binding DetectedAt, StringFormat='{}{0:HH:mm:ss}'}" Width="70"/>
                <DataGridTextColumn Header="Process" Binding="{Binding ProcessName}" Width="120"/>
                <DataGridTextColumn Header="PID" Binding="{Binding Pid}" Width="60"/>
                <DataGridTextColumn Header="Direction" Binding="{Binding Direction}" Width="100"/>
                <DataGridTextColumn Header="Source" Binding="{Binding SourcePath}" Width="*"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="70"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
